{
  "cliOpts": {
    "template": {
      "options": ["frontend", "backend", "web", "native"],
      "default": "web"
    },
    "project": {
      "default": "project-template"
    },
    "install": {
      "options": ["true", "false"],
      "default": true
    },
    "interactive": {
      "default": false
    }
  },
  "ignoreFolders": ["node_modules", "coverage", "dist", "lib"]
}
